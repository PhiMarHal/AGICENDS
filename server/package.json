{
  "name": "agicends-server",
  "version": "0.1.0",
  "private": true,
  "description": "AGICENDS multiplayer server.",
  "main": "server.js",
  "scripts": {
    "start": "node --env-file-if-exists=.env server.js",
    "test": "node --test test/*.test.cjs",
    "benchmark": "node benchmark.cjs"
  },
  "dependencies": {
    "express": "^4.21.0",
    "ws": "^8.18.0"
  },
  "engines": {
    "node": ">=24"
  }
}
